// Options for the GulpRefilename plugin.
export interface GulpRefilenameOptions {
	basename?: string;
	dirname?: string;
	extname?: string;
	multiExt?: boolean;
	prefix?: string;
	suffix?: string;
}
