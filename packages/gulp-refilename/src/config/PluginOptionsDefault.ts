import type { GulpRefilenameOptions } from '../schema';

const defaultOptions: GulpRefilenameOptions = {
  dirname: '',
  stem: '',
  extname: '',
  prefix: '',
  suffix: '',
};

export default defaultOptions;
